# Server configuration
PORT=3002

# MongoDB connection string (use MongoDB Atlas in production)
MONGO_URL=mongodb://localhost:27017/tradesphere

# JWT authentication
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRY=7d

# Allowed frontend URLs for CORS (comma-separated)
# Example production:
# CLIENT_URL=https://tradesphere.vercel.app,https://tradesphere-dashboard.vercel.app